# 수원 바를정한의원 홈페이지 배포 · 어드민 설치 순서 (GitHub → Cloudflare Pages)

울산 정한의원 · 후한의원과 같은 방식입니다. **소스는 GitHub, 배포는 Cloudflare Pages(Git 연결)**, 어드민은 같은 저장소의 **`admin` 브랜치**를 별도 Cloudflare Pages 프로젝트로 올리고 `_worker.js` 로 아이디·비밀번호를 겁니다.

| 항목 | 값 |
|---|---|
| GitHub 저장소 | `gkwngml1215-code/sw-bareul` (Private) |
| 홈페이지 | `main` 브랜치 → Cloudflare Pages 프로젝트 `sw-bareul` → **https://www.sw-bareul.co.kr** |
| 어드민 | `admin` 브랜치 → Cloudflare Pages 프로젝트 `sw-bareul-admin` → `https://sw-bareul-admin.pages.dev` |
| 도메인 | sw-bareul.co.kr (구매 후 DNS 는 Cloudflare 로 이전) |
| 내 PC 폴더 | `수원 바를정한의원\sw-bareul` |

## 진행 현황 (2026-09-21 기준)

| 단계 | 상태 |
|---|---|
| 1단계 GitHub 저장소 | 완료 (`main` · `admin` 브랜치) |
| 2단계 홈페이지 프로젝트 | 완료 — `https://sw-bareul.pages.dev`, `main` 푸시 시 1~2분 내 자동 배포 |
| 3단계 도메인 연결 | 완료 (2026-09-21) — 가비아 네임서버를 `aurora.ns.cloudflare.com` / `bruce.ns.cloudflare.com` 으로 변경, Custom domain `www.sw-bareul.co.kr` · `sw-bareul.co.kr` 연결, Redirect Rule `root to www` (301) 배포 |
| 4단계 어드민 프로젝트 | 완료 — `https://sw-bareul-admin.pages.dev` 아이디·비밀번호 잠금 작동 |
| 5단계 GitHub 접속 키 | 어드민 로그인 · 화면 확인 완료 (2026-09-21) |
| 검색엔진 등록 | 완료 (2026-09-21) — 네이버 서치어드바이저 · 구글 서치콘솔 소유확인(HTML 태그, 코드는 어드민 "검색 설정"에 저장) + `sitemap.xml` 제출 |
| 배포 후 할 일 | 미완료 — 네이버 플레이스 · 카카오 채널 · 유튜브 · 블로그 홈페이지 주소 변경, 기존 사이트(danaan-sw.com) 리다이렉트 요청 |

- 도메인 등록처: 가비아 (도메인은 가비아에 그대로, DNS 만 Cloudflare)
- Cloudflare 도메인 추가 시 **Bot Preference Sync 는 끔** (robots.txt 는 빌드가 직접 생성)

이 사이트는 게시글 원고(JSON)에서 페이지를 **Cloudflare 가 빌드할 때** 만듭니다. 그래서 홈페이지 프로젝트에 빌드 명령이 들어갑니다 (2단계 표 참고).

전부 무료이고, 처음 한 번만 하면 됩니다. **1단계부터 순서대로** 해 주세요.

---

## 1단계 — GitHub 저장소 만들기

이 PC 에는 이미 git 저장소(`main` · `admin` 브랜치)가 만들어져 있습니다. GitHub 에 빈 저장소만 만들고 올리면 됩니다.

1. https://github.com/new → **Repository name** `sw-bareul` → **Private** → README 등 추가 옵션은 모두 체크 해제 → **[Create repository]**
2. 사이트 폴더에서 아래 두 줄 실행 (또는 Claude 에게 "GitHub 에 올려줘")

   ```
   git push -u origin main
   node tools/sync-admin.js
   ```

3. `https://github.com/gkwngml1215-code/sw-bareul` 에 `main` 과 `admin` 두 브랜치가 보이면 완료.

> 푸시할 때 로그인을 물으면 아이디 `gkwngml1215-code`, 비밀번호 자리에 `★중요 정보_지우지말것.txt` 의 GitHub 퍼미션 토큰을 넣으세요.

---

## 2단계 — 홈페이지 프로젝트 만들기 (Cloudflare Pages ← GitHub main)

1. https://dash.cloudflare.com 로그인 (`★중요 정보_지우지말것.txt` 의 클라우드플레어 계정) → 왼쪽 **Workers & Pages**
2. **[Create]** → 위쪽 **Pages** 탭 → **[Connect to Git]**
3. **GitHub** 선택 → 저장소 목록에 **`sw-bareul`** 이 없으면 [Configure GitHub App] 에서 `sw-bareul` 을 추가로 체크
4. 저장소 **`sw-bareul`** 선택 → **[Begin setup]**
5. 아래처럼 입력합니다 (**가장 중요한 단계**)

   | 항목 | 넣을 값 |
   |---|---|
   | Project name | `sw-bareul` (주소가 `sw-bareul.pages.dev`) |
   | Production branch | **`main`** |
   | Framework preset | **None** |
   | Build command | **`node tools/build.js`** |
   | Build output directory | **`dist`** |

6. **[Save and Deploy]** → 1~2분 뒤 "Success" 가 뜨면 `https://sw-bareul.pages.dev` 에서 홈페이지가 보입니다.

> Build command 나 output directory 를 비워두면 게시글 페이지가 만들어지지 않고 어드민 파일이 그대로 노출됩니다. 꼭 위 표대로 넣어 주세요.

7. (권장) 프로젝트 → **Settings** → **Builds & deployments** → **Branch control** → **Preview branch: None** → Save. (admin 브랜치가 홈페이지 미리보기로 빌드되지 않게)

---

## 3단계 — 도메인 연결 (sw-bareul.co.kr)

Cloudflare Pages 에 도메인을 붙이려면 도메인의 DNS 를 Cloudflare 가 맡아야 합니다. 도메인을 구매한 곳(가비아 등)에서 네임서버만 바꿔 줍니다.

1. Cloudflare 대시보드 → **[Add a domain]** → `sw-bareul.co.kr` 입력 → **Free** 플랜 → Continue
2. 네임서버 2개를 메모 (예: `xxx.ns.cloudflare.com`, `yyy.ns.cloudflare.com`)
3. 도메인 구매처 → 도메인 관리 → `sw-bareul.co.kr` → **네임서버 설정** → 1차·2차에 위 두 개를 넣고 저장 (반영까지 몇 분~몇 시간)
4. Cloudflare 에서 "Active" 로 바뀌면 → **Workers & Pages** → 프로젝트 **`sw-bareul`** → **Custom domains** → **[Set up a custom domain]** → `www.sw-bareul.co.kr` → Continue → Activate.
5. 한 번 더 **[Set up a custom domain]** → `sw-bareul.co.kr` (www 없이) → Activate.
6. (권장) `sw-bareul.co.kr` 도메인 → **Rules** → **Redirect Rules** → 템플릿 **"Redirect from Root to WWW"** → Deploy.
7. https 인증서는 Cloudflare 가 자동으로 붙입니다. `https://www.sw-bareul.co.kr` 이 열리면 완료.

---

## 4단계 — 어드민 프로젝트 만들기 (Cloudflare Pages ← GitHub admin)

1. **Workers & Pages** → **[Create]** → **Pages** → **[Connect to Git]** → 저장소 **`sw-bareul`** → **[Begin setup]**
2. 입력:

   | 항목 | 넣을 값 |
   |---|---|
   | Project name | `sw-bareul-admin` (주소가 `sw-bareul-admin.pages.dev`) |
   | Production branch | **`admin`** ← 반드시 admin (main 아님) |
   | Framework preset | **None** |
   | Build command | (비워둠) |
   | Build output directory | **`/`** |

3. **[Save and Deploy]**
4. 프로젝트 → **Settings** → **Builds & deployments** → **Branch control** → Production branch `admin`, **Preview branch: None** → Save
5. **Settings** → **Variables and Secrets** → **[Add]** 로 2개 추가

   | 이름 | 값 | 종류 |
   |---|---|---|
   | `ADMIN_USER` | 원하는 아이디 | **Secret** |
   | `ADMIN_PASS` | 원하는 비밀번호 | **Secret** |

6. **[Save]** → **Deployments** 탭 → 최신 배포 **[...]** → **[Retry deployment]**
7. 시크릿 창(Ctrl+Shift+N)으로 `https://sw-bareul-admin.pages.dev` 접속 → **아이디·비밀번호 입력창이 뜨면 잠금 성공**

| 이런 화면이 나오면 | 원인 | 해결 |
|---|---|---|
| 입력창 없이 바로 열림 | `_worker.js` 가 적용 안 됨 | Build output directory 가 `/` 인지 확인 후 재배포 |
| `설정 오류: ... ADMIN_USER ...` | 환경 변수 이름이 틀림 | 5번 표와 한 글자씩 대조 |
| 맞는 비번인데 계속 물어봄 | 재배포를 안 함 | 6번(Retry deployment) |
| 홈페이지가 보임 | Production branch 가 main | 2번을 admin 으로 |

---

## 5단계 — GitHub 접속 키 만들기 (어드민 안에서 쓰는 키)

1. https://github.com/settings/personal-access-tokens → **[Generate new token]**

   | 항목 | 넣을 값 |
   |---|---|
   | Token name | `바를정한의원 어드민` |
   | Expiration | **No expiration** |
   | Repository access | **Only select repositories** → **`sw-bareul`** 선택 |
   | Permissions → Repository permissions → **Contents** | **Read and write** |

2. **[Generate token]** → `github_pat_` 로 시작하는 문자열을 복사
3. `https://sw-bareul-admin.pages.dev` → 아이디·비밀번호 → 관리 화면 **왼쪽 메뉴 맨 아래 "🔑 GitHub 접속 키"** 칸에 붙여넣고 **[저장]**
4. 상태가 "연결됨 ✓" 으로 바뀌고 게시글 목록에 **옮겨 놓은 글 67편**이 보이면 끝입니다.

---

## 이제 이렇게 씁니다

1. 어디서든 `https://sw-bareul-admin.pages.dev` → 아이디·비밀번호 → (처음 한 번) 접속 키
2. **새 글 쓰기** → 카테고리(바를정 칼럼 / 치료 전후 사례 / 환자수기 / 언론보도 / 공지사항) 선택 → 작성 → **저장하고 공개**
3. 확인창에서 **새 글/수정, 제목, 상태, 주소, 올라가는 사진 개수**를 보고 **확인**
4. "GitHub 저장 완료 · 홈페이지 반영 중…" → 1~2분 뒤 "홈페이지 반영 완료 ✔" → **미리보기**
5. 사진은 올리기 전에 자동으로 웹용 크기(긴 변 1600px)로 줄어듭니다.

- 목록은 항상 **GitHub 에 올라가 있는 글** 기준입니다. 다른 PC·폰에서도 똑같이 보입니다.
- 글 주소(슬러그)가 이미 있는 글과 겹치면 저장이 멈춥니다. 다른 글을 덮어쓰는 일은 없습니다.
- **삭제**는 [삭제] 버튼 → 확인창을 눌렀을 때만 됩니다.
- 임시저장은 GitHub 에 저장되지만 홈페이지에는 보이지 않습니다.

## 홈페이지 설정 (어드민 메뉴)

어드민 왼쪽 **🏥 홈페이지 설정** 에서 아래 값을 고치고 **저장하고 홈페이지에 반영** 을 누르면 1~2분 뒤 홈페이지 전체에 한꺼번에 반영됩니다.

- 기본 정보: 병원명 · 대표원장 · 전화 · 이메일 · 사업자등록번호 · 외래 주소 · 입원실 1·2병동 주소 · 주변 안내 · 주차 안내
- 진료시간: 월~금 · 토요일 · 일요일/공휴일 · 점심시간 · 휴진 안내 · 안내 문구
- 예약 · 채널: 네이버 예약 · 카카오톡 채널 · 블로그 · 네이버 플레이스 · 카카오 지도 · 유튜브 · 인스타그램 · 네이버 카페
- 검색 설정: 메인 페이지 제목·설명, 네이버·구글 소유확인 코드
- 공지 띠: 홈페이지 맨 위 한 줄 공지 (연휴 진료 안내 등)

## 홈페이지 · 어드민을 고쳤을 때

- **홈페이지 문구·사진** (게시글 이외): 이 PC 에서 Claude 와 고친 뒤 "GitHub 에 올려줘" → `main` 에 푸시되면 Cloudflare 가 1~2분 뒤 자동 배포합니다.
- **어드민 화면**: `admin/` 폴더를 고친 뒤 `node tools/sync-admin.js` 실행 → `admin` 브랜치에 올라가고 자동 배포됩니다.
- 배포 기록·되돌리기: Cloudflare 프로젝트 → **Deployments** 탭

**`admin/_worker.js` 는 절대 지우거나 이름을 바꾸지 마세요.** 이 파일이 아이디·비밀번호 잠금입니다.

---

## 배포 후 할 일

- 네이버 서치어드바이저 (https://searchadvisor.naver.com) · 구글 서치콘솔 (https://search.google.com/search-console) 에 `https://www.sw-bareul.co.kr` 등록 → `https://www.sw-bareul.co.kr/sitemap.xml` 제출
- 네이버 플레이스 · 카카오 채널 · 유튜브 · 블로그 프로필의 홈페이지 주소를 새 도메인으로 변경
- 기존 홈페이지(danaan-sw.com)에서 새 주소로 리다이렉트 설정 (기존 업체에 요청)
- 치료 전후 사진 · 환자수기는 환자 동의 · 의료광고 심의 확인
